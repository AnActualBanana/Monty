#include "monty.h"

/* this function will push an element to the stack, (it will probably go to the end of the list, then add a new node) error cases for this function will be captured in the reader
 */

